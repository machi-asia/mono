feature(calculator): add Create mod game with recipes and item textures
- calculator: add new game dataset for Create (`create`) with 1,200+ items and 1,400+ recipes extracted from mod jar
- calculator: extract 1,250+ item and block textures to `apps/calculator/public/create/items/`
- calculator: register Create dataset in `src/data/games.ts` using items.xml schema
- calculator: fix `.calc-recipe-select` CSS class targeting from legacy `.dropdown-*` to design system `.m-dropdown*`, constraining max width and ensuring text truncation
- calculator: add `flex-shrink: 0` and `white-space: nowrap` to `.calc-item-actions-group` so action buttons do not overlap
- calculator: add `max-width`, `white-space: nowrap`, and `text-overflow: ellipsis` to `.calc-rate-badge`

