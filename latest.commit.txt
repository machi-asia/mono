chore(repo): expand gitignore for nextjs builds, env files, and artifacts

- repo: updated `.gitignore` with standard rules for build output (`.next`, `dist`, `build`, `out`, `coverage`), TypeScript cache, environment files (`.env*`), logs, and editor artifacts.
