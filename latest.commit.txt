feature(calculator): add multi-game calculator app and recipe tree calculation graph
- apps/calculator: implement interactive recipe tree calculation graph with React Flow and Dagre layout, calculating required machines, rates per minute, conveyor belt saturation, and raw ingredient demands
- apps/calculator: fix ReactFlow graph nodes not rendering -- replace Card (section element) as RecipeProcessNode root with a plain div; supply explicit width/height on node data to bypass ResizeObserver measurement pass that left all nodes visibility:hidden
- apps/calculator: fix height chain for graph view -- add flex:1 and flex-direction:column to calc-main-graph so the calc(100vh-140px) recipe-graph-container has a real parent height
- apps/calculator: add Google AdSense right-side ad rail -- fixed 180px sticky sidebar with 160x600 skyscraper unit; placeholder striped panel shown when unconfigured; hidden below 1280px
- apps/calculator: gate AdSense to production only and fix rail ad sizing -- loader script, google-adsense-account meta, and adsbygoogle <ins> are rendered only when NODE_ENV=production (dev console no longer floods with doubleclick/recaptcha partitioned-cookie + OpaqueResponseBlocking warnings); rail unit now uses fixed 160x600 dimensions with data-full-width-responsive=false so AdSense auto-size stops writing empty width/height inline styles (the "Declaration dropped" CSS errors and 0x0 no-fill requests with negative coordinates); drop stale biome/eslint eslint-disable directives in adsense-script.tsx
- apps/calculator: AdSenseScript server component placed as sibling of <body> inside <html> -- outside React client hydration tree entirely, so no 'script tag in component' warning and no data-nscript attribute; google-adsense-account meta tag emitted via Next.js metadata pipeline for account verification
- apps/calculator: env vars NEXT_PUBLIC_ADSENSE_PUBLISHER_ID and NEXT_PUBLIC_ADSENSE_SLOT_RAIL with .env.sample and .env.local; Supabase keys copied from shared project
- apps/calculator: LCP optimization -- first catalog item image uses loading=eager and priority=true
- apps/calculator: fix AdSenseUnit ins element width/height using px string values instead of bare numbers to avoid CSS parse errors
- apps/calculator: wrap React Flow in ReactFlowProvider with responsive grid container sizing and automatic fitView centering upon target updates
- apps/calculator: add dual view switcher between Catalog View and Recipe Tree Graph View with direct Graph actions from item cards
- apps/calculator: pure calculation engine in graph-builder.ts with cycle-detection safeguards for complex recipe loops
- apps/calculator: integrate Little Rocket Lab as first game from kamachi-dev/lrl_calculator, organizing items in data/lrl/items.xml and item photos in /public/lrl/items/
- apps/calculator: modularize game item datasets into folders by file (data/<game>/items.xml) with XML parsing utility; Vitest test suite (11 passing tests)
- packages/components: fix next-themes React 19 script tag warning by adding scriptProps={{ suppressHydrationWarning: true }} to NextThemesProvider
- packages/components: break cyclic build dependency between @mono/components and @mono/auth; configure Vitest pool: threads
- packages/auth, packages/components, packages/database, packages/rose: align TypeScript to ^6.0.3 to eliminate TypeScript v7 Go native concurrent watch deadlock (-1073741523)
- packages/rose: fix voice overlay stylelint issues; add 'use client' directive for Turbopack compatibility
- docs: add ADR 014, update ARCHITECTURE.md, README.md, CHANGELOG.md for release 0.19.0
