feature(branding): add shared logo and set it as favicon on all apps

- assets/branding/: new shared location with the primary logo (logo.svg + logo.png) so all packages/apps can reuse it from one source of truth
- apps (docs/rose/machi-asia): added src/app/icon.png so each Next.js site serves the brand logo as its favicon automatically (App Router icon convention -> /icon.png); verified via builds
