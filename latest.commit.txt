feature(components): live interactive component showcases with per-prop dropdowns

- components: `ComponentShowcase` now renders each component live via a `render(values)` function and a `propControls` dropdown for every choice/enum prop (replaces the static `demo` node), backed by stateful per-item selects that re-invoke `render` on change.
- auth: added `MockAuthProvider` (`@mono/auth/mock`) so context-dependent auth components render live without a backend; exported `AuthContext` for reuse.
- docs: auth and components showcase pages now render live client-side demos (mock auth-state dropdown for `AuthGate`, embedded `SignInModal`, `packageName` dropdown for `ComponentShowcase`) via `"use client"` wrappers.
- docs: convention enforced in markdowns (`AGENTS.md` rule 12, `ARCHITECTURE.md`, `CONTRIBUTING.md`, `DESIGN.md`, `README.md`, `ADR-004`) so every exported component must render live with a per-prop dropdown on every create/update.
- components: `packageName`/`description` are now the only top-level props; per-component interactive controls moved into `propControls`.
- test: added `ComponentShowcase` dropdown render/re-render tests and `MockAuthProvider`-driven `AuthGate`/`SignInModal` tests; full suite green (lint, stylelint, typecheck, vitest).
- chore: added `npm run deploy` (`scripts/deploy.js`) running `git add .` → `git commit -F latest.commit.txt` → `git push`, aborting if any step fails; validates that `latest.commit.txt`'s first line matches the Conventional-Commits title regex before committing.
- docs: `AGENTS.md` documents the `latest.commit.txt` title format and bullet-per-change convention.

## Component Showcase Convention (previous session)

- components: `ComponentShowcase` list-view layout exported; showcase `page.tsx` per package in the docs app.
- docs: `apps/docs/src/app/components/{auth,components,database}/page.tsx` showcase pages, linked from the docs home.
- docs: rule added to `AGENTS.md` (rule 12), `ARCHITECTURE.md`, `CONTRIBUTING.md`, `DESIGN.md`, `README.md`, and `ADR-004`.

## Auth Enforcement (previous session)

- auth: `AuthProvider`, `AuthGate`, `SignInModal` (no exit/cancel; Google OAuth, email/password, Continue as Guest via `signInAnonymously`), `signInWithGoogle`, `createClient`, server/middleware, `useAuth`.
- apps: all three apps (`machi-asia`, `rose`, `docs`) wrap root layout in `AuthProvider` + `AuthGate`; depend on `@mono/auth`; real Supabase credentials in each `.env.local`.
- env: `scripts/check-env.js` placeholder regex refined so `npm run env` passes.
- tooling: packages resolve from `src` TypeScript instead of pre-built `dist` (fixes `Module not found`/CSS-in-dist issues); stale `dist`/`.next` caches removed; vitest `globals: true` + `exclude: dist`.
- docs: `CHANGELOG.md` (`0.3.0`/`0.4.0`/`0.5.0`), `docs/ARCHITECTURE.md`, ADRs `003`/`004`.

Note: `next build` (Turbopack) cannot run on this machine (Application Control blocks the native SWC/Turbopack binary); dev uses `next dev --webpack`.
